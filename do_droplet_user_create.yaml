---
- hosts: "digitalocean"
  become: yes
  vars:
    user: gangaram
    group: gangaram
    password: WPda5UVp4x4uk   #password hash generated by mkpass (Install Step: sudo apt install whois -y). Ex: for password "abcd", the hash is WPda5UVp4x4uk   
    public_key: "<PUB_KEY>" 
  roles:
    - do_droplet_user_create
  tags:
    - add-user

- hosts: "digitalocean"
  become: yes
  vars:
    user: gangaram
  tasks:
    - user: name="{{ user }}" state=absent remove=yes
  tags:
    - delete-user